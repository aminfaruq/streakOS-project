//
//  AppColors.swift
//  StreakOSApp
//
//  Design-system color tokens (mirrors mockup-v2 design constants).
//

import SwiftUI

enum AppColors {

    // Backgrounds
    static let background  = Color("Background")
    static let card        = Color("Card")

    // Accents
    static let accent     = Color("AccentGreen")
    static let blue       = Color("BlueAccent")

    // Neutral text / borders
    static let label      = Color.primary
    static let secondary  = Color.secondary

    // Surface tints
    static let surfaceTint = Color.gray.opacity(0.15)
}
